{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build CFFI Test",
            "type": "shell",
            "command": "pushd ffi && make all && popd",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}